{
  "name": "api-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "Chrome-run": "npx playwright test tests/E2E.spec.ts --project=chromium --reporter=html",
    "test:smoke": "playwright test --project=chromium --reporter=html --grep @smoke",
    "test:regression": "playwright test --project=chromium --reporter=html --grep @regression"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^24.7.2"
  }
}
